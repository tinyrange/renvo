# Compiler audit repair validation history

This document preserves the validation history recorded in draft PR #460 through
commit `1b2c0b48`. Entries describe the state at the time of each repair; later
entries supersede earlier audit counts. They do not establish complete Go
compatibility. The active remaining-work checklist stays in
[PR #460](https://github.com/tinyrange/renvo/pull/460).

## Validation

Validation during this repair series was primarily on Darwin/arm64:

- Rebased the initial eight repair commits onto main at `4862e9fc`, resolving only generated-output conflicts by regeneration. Latest `./tools/check preflight` passed after numeric-builtin validation and dispatch repairs in 37 seconds (60-second budget).
- Newer review rerun: 92/92 valid fixtures matched recorded results and all 16 invalid fixtures were rejected. Built-in println booleans were normalized; fmt output was compared exactly.
- Original 100-case negative-source audit rerun with the host frontend: 50 checker diagnostics, 25 backend-only failures, one linker-only failure, 22 false accepts, and two compiler crashes (negative constant shift and recursive value type). These outcomes are not counted as a passing negative-source gate; permanent rejection coverage and repairs remain required.
- Subsequently repaired the recursive-value-type crash: the original case now gets `RENVO-CHECK-037`. Seven checked-in rejection fixtures pass host and stage3; checker tests cover nine valid indirect/shared type shapes.
- Constant-operation checking fixes the original negative-shift crash and division-by-zero false accept; both now get `RENVO-CHECK-038`. Five checked-in rejection fixtures pass host and stage3, with checker controls for valid expressions, lexical shadowing, and arithmetic overflow. The refreshed original 100-case host audit now has 53 checker diagnostics, 25 backend-only failures, one linker-only failure, 21 false accepts, and no crashes in that fixture set. The negative-source gate remains incomplete.
- Struct-literal checking adds five host/stage3 rejection fixtures. Before the main rebase, the complete checked-in negative corpus passed host and stage3 after this change, alongside checker controls for valid literals and local map-type shadowing.
- Five array-length rejection fixtures pass host and stage3; arithmetic/checker tests cover large cancellation, shifts, multiplication, signed rounding, and integer boundaries. The latest original 100-case host audit with an explicit development std root has 59 checker diagnostics, 24 backend-only failures, one linker-only failure, 16 false accepts, and no crashes.
- Repaired the four post-rebase stage3 loader failures (`audit_goto_garbage`, `excluded_channel`, `excluded_goroutine`, `excluded_select`) by bundling the required core runtime. The full checked-in negative corpus is green again on host and stage3. A new program without runtime imports/replacements exercises buffered channels, goroutine handoff, and closed-channel receive on host and stage3. Bundle tests pass with optional extras enabled and disabled.
- Four unary-literal rejection fixtures and the full checked-in negative corpus pass host and stage3. A positive regression covers valid unary operations, string indexing, and nested-parenthesis comparisons on both compilers. The original `t_neg_not_int`, `t_neg_bitnot_float`, `t_neg_deref_int`, and `t_neg_neg_string` cases now produce `RENVO-CHECK-030`; later full original-audit reruns are recorded below.
- Complex-literal ordering and package-initializer rejection fixtures pass host and stage3, alongside a positive control for complex equality, real ordering, and zero-imaginary conversion to typed real operands. The complete checked-in negative corpus is green. The refreshed original 100-case host audit now has 64 checker diagnostics, 23 backend-only failures, one linker-only failure, 12 false accepts, and no crashes; it is still not a passing negative-source gate.
- Three bare-return shadowing negative fixtures and the complete checked-in negative corpus pass host and stage3. A positive scope regression exposed an explicit-return miscompile, reduced to `named_result_explicit_shadow.go`: stage0 printed `FAIL` before the backend fix and `PASS` afterward. All three focused backend cases pass direct-source and frontend-unit paths, and the frontend positive passes host/stage3. The latest original host audit has 65 checker diagnostics, 23 backend-only failures, one linker-only failure, 11 false accepts, and no crashes. The resource gate was rerun after this backend change and still fails at 657,010 bytes.
- Two additional stage0 reproducers cover named-result swaps (wrong output before the fix) and immediately invoked result-bearing closures (compilation failure before the parser fix). Five focused backend programs pass both direct-source and frontend-unit paths. The host/stage3 frontend regression covers integer/string/array swaps and closure/defer ordering; the complete checked-in negative corpus remains green. The resource gate was rerun and still fails at 657,010 bytes; no broader checklist item is closed by these focused repairs.
- Four capacity-builtin rejection fixtures and a valid array/pointer-to-array/slice/channel/shadowing regression pass host and stage3. The complete checked-in negative corpus remains green. The original host audit is now 66 checker diagnostics, 23 backend-only failures, one linker-only failure, and 10 false accepts, with no crashes.
- Four explicit interface-method duplicate fixtures pass host/stage3, as does a shared-embedding dispatch regression. The complete checked-in negative corpus is green. The original host audit now records 67 checker diagnostics, 23 backend-only failures, one linker-only failure, and 9 false accepts, with no crashes.
- Three call-operand rejection fixtures and the complete checked-in negative corpus pass host/stage3. The positive regression verifies tuple assignment, return forwarding, and a containing call used in arithmetic. Unit controls cover imported functions, parenthesized calls, shadowing, trailing result commas, and newline statement boundaries. The latest original host audit is 68 checker diagnostics, 23 backend-only failures, one linker-only failure, and 8 false accepts, with no crashes.
- Three primitive map-literal rejection fixtures and the complete checked-in negative corpus pass host/stage3. A positive regression covers named map/key types, aliases, package initializers, and string-index values. Unit controls include local type shadowing, interface-valued maps, and integral floating constants. The original host audit now has 70 checker diagnostics, 23 backend-only failures, one linker-only failure, and 6 false accepts, with no crashes.
- Three map-index rejection fixtures and the complete checked-in negative corpus pass host/stage3. The positive regression covers named/global/parameter maps, inferred aliases, block shadowing, and control-initializer scope; it crashed before the lowering repair and now prints PASS. The 158-case focused map/function-value set passes host/stage3; final preflight passes in 35 seconds. The refreshed original host audit records 71 checker diagnostics, 23 backend-only failures, one linker-only failure, and 5 false accepts, with no crashes.
- Three map-field assignment rejection fixtures and the complete negative corpus pass host/stage3. The positive regression covers pointer-map field mutation, nested pointer fields, named pointer maps, copied struct values, and whole-element replacement. Checker controls cover aliases, shadowing, parenthesized/multi-target assignments, and pointer-valued aggregate literals. Preflight passes in 47 seconds. The refreshed original host audit has 72 checker diagnostics, 23 backend-only failures, one linker-only failure, and 4 false accepts, with no crashes.
- Three pointer-ordering rejection fixtures and the complete checked-in negative corpus pass host/stage3. Positive controls verify pointer equality, aliases, named pointers, nested dereferences, and lexical shadowing; existing map regressions also pass after sharing declaration resolution. Checker tests cover address expressions, function results, pointer conversions, builtin/type shadowing, and closure-parameter isolation. Final preflight passes in 38 seconds. The refreshed original host audit records 73 checker diagnostics, 23 backend-only failures, one linker-only failure, and 3 false accepts, with no crashes.
- Three interface-compatibility rejection fixtures and the complete negative corpus pass host/stage3. Positive interface dispatch, aliases, assertions, pointer-receiver updates, and shared embedding regressions pass both compilers. Checker controls preserve local and closure shadowing and unresolved promotion. Preflight passes in 50 seconds. The refreshed original host audit has 75 checker diagnostics, 23 backend-only failures, one linker-only failure, and one false accept (`close(1)`), with no crashes; the legacy integer-close API conflict remains explicitly open.
- Three array-literal rejection fixtures and the complete checked-in negative corpus pass host/stage3. The positive regression verifies sparse/reset positions, inferred length, named arrays/aliases, global literals, and empty arrays. Checker controls include huge intermediate constants and local shadowing. Preflight passes in 50 seconds. The refreshed original host audit records 76 checker diagnostics, 22 backend-only failures, one linker-only failure, and one false accept, with no crashes.
- Four make-allocation rejection fixtures and the complete checked-in negative corpus pass host/stage3. The positive regression verifies named slices/maps, capacity and zero initialization, channels, anonymous struct elements, and a shadowed make function. Checker tests cover oversized/negative/fractional sizes, capacity ordering, aliases, and local shadowing. Preflight passes in 50 seconds. The refreshed original host audit has 78 checker diagnostics, 20 backend-only failures, one linker-only failure, and one false accept, with no crashes.
- Four numeric-builtin rejection fixtures and the complete checked-in negative corpus pass host/stage3. Positive numeric components, named float/complex types, constant conversion, and local function shadowing pass both compilers. Two minimal backend programs failed to compile under the prior stage0 and now print PASS; both pass direct-source and frontend-unit paths, alongside eight existing complex-number controls. Final preflight passes in 37 seconds. The resource gate was rerun after the backend repair and still fails at 657,010 bytes. The refreshed original host audit has 80 checker diagnostics, 18 backend-only failures, one linker-only failure, and one false accept, with no crashes.
- Focused backend regressions passed direct-source and frontend-unit compilation, including escape/allocation cases.
- 158 focused function-value frontend cases passed host and stage3 compilation.
- Named-integer/string map regression with 100,000 entries passed host and stage3, alongside existing integer-map and general map-lowering controls. Covers deletion, moved entries, reinsertion, clearing, nil maps, embedded NUL/Unicode strings, and independently allocated equal strings. Linker package tests passed.
- `unsafe.Add` positive coverage and four negative fixtures passed host and stage3; exact decimal-fraction classification unit tests passed.
- `unsafe.String`/`StringData` storage and panic regression and four negative fixtures passed host and stage3 on Darwin/arm64. Unsigned-word constant and same-low-word/different-high-word branch regressions pass native direct-source and frontend-unit paths, direct WASI execution, and a checked-in 32-bit VM execution test. Fixed target-branch pruning and small-constant recognition to preserve complete literal values; signedness recognition is no longer architecture-dependent. This closes the previously recorded WASI wide-constant branch failure, not the broader cross-target audit.
- Existing large-slice zeroing code-size bounds and prepared MS-DOS/VM evaluator checks passed without relaxing limits.
- AArch64 scaled-offset encoding/boundary tests passed. Distant scalar fields, narrow-field comparisons, tagged memory access, and operand-order controls passed all four direct-source and four frontend-unit cases. The distant-field reproducer failed on the prior compiler before the comparison fix.
- The Darwin/arm64 compiler resource gate was rerun and **fails binary size** at 657,010 bytes against the unchanged 327,680-byte limit. The pre-PR main baseline (208be4d9) also fails at 624,178 bytes; the map-hashing baseline measured 640,594 bytes. The addressing optimization reduces emitted text slightly but does not change the aligned executable size. Full validation and the remaining performance/resource gates have not been run. Focused passes do not establish compliance with unexecuted gates.

- Length/capacity scalar validation: the complete checker suite and checked-in negative corpus pass; two new rejection fixtures and three focused positive regressions pass host/stage3. Preflight passes in 46 seconds. The refreshed original host audit records 81 checker diagnostics, 17 backend-only failures, one linker-only failure, one false accept, and no crashes. No backend implementation changes or new resource-gate measurement in this batch.

- Range operands: the complete checker/linker suites and checked-in negative corpus pass. Three rejection fixtures and a positive regression covering named integer conversions, strings, containers, and lexical shadowing pass host/stage3. The positive initially failed on `Count(3)` before the inference repair. Final preflight passes in 36 seconds; an earlier run passed individual checks but exceeded the budget at 75 seconds. The refreshed original host audit records 83 checker diagnostics, 15 backend-only failures, one linker-only failure, one false accept, and no crashes. No backend implementation changes or new resource-gate measurement in this batch.

- Struct ordering: the complete checker suite and checked-in negative corpus pass. Three new rejection fixtures and the positive struct, pointer, and range control programs pass host/stage3. Final preflight passes in 43 seconds. The refreshed original host audit records 84 checker diagnostics, 14 backend-only failures, one linker-only failure, one false accept, and no crashes. No backend implementation changes or new resource-gate measurement in this batch.

- Shift operands: the complete checker suite and checked-in negative corpus pass. Three new rejection fixtures and valid integral-constant, precedence, and lexical-shadowing controls pass host/stage3. The stage0 integral-decimal-shift reproducer failed to compile before the repair; two new backend programs and two existing unsigned-constant controls pass direct-source and frontend-unit paths. Exact decimal digits are used without IEEE rounding, within the covered compiler-word/literal bounds. Final preflight passes in 44 seconds. The refreshed original host audit records 86 checker diagnostics, 12 backend-only failures, one linker-only failure, one false accept, and no crashes. The resource gate was rerun after the backend changes and still fails at 657,010 bytes against 327,680 bytes.

- Copy/delete operands: the complete checker/linker suites and checked-in negative corpus pass. Three new rejection fixtures and a positive regression covering named slices/maps, string copying, nil-map deletion, and shadowing pass host/stage3. The positive initially failed on an adjacent named-map declaration before the local-type lookup repair; a linker unit test reproduces that lookup miss. All 320 selected map/function-value cases pass both host and stage3. Final preflight passes in 46 seconds. The refreshed original host audit records 88 checker diagnostics, 10 backend-only failures, one linker-only failure, one false accept, and no crashes. No backend implementation changes or new resource-gate measurement in this batch.

- Append operands: the complete checker suite and checked-in negative corpus pass. Three new rejection fixtures and valid empty/expanded/string/shadowing append controls pass host/stage3. Three minimal backend regressions cover nil expansion (compile failure before repair), slice parameter `nil` shadowing (wrong output), and short-declaration source evaluation (called twice before repair). The broad run passed 105 direct-source and 106 frontend-unit cases; a focused follow-up passed all three new programs on both paths, covering the additional direct-source case. Final preflight passes in 47 seconds. The original host audit now records 89 checker diagnostics, 9 backend-only failures, one linker-only failure, one false accept, and no crashes. The resource gate was rerun after the backend changes and still fails at 657,010 bytes against 327,680 bytes.

- Assignment targets: the complete checker suite and checked-in negative corpus pass. Three new rejection fixtures and valid shadowing, pointer, returned-slice, and control-scope assignment controls pass host/stage3. Shared scoped bindings now distinguish writable declarations from local constants/types. Final preflight passes in 47 seconds. The refreshed original host audit records 91 checker diagnostics, 7 backend-only failures, one linker-only failure, one false accept, and no crashes. No backend implementation changes or new resource-gate measurement in this batch.

- Index operands: the complete checker suite and checked-in negative corpus pass. Three new rejection fixtures and valid string/container/pointer-to-array/shadowing controls pass host/stage3. Final preflight passes in 44 seconds. The refreshed original host audit records 92 checker diagnostics, 6 backend-only failures, one linker-only failure, one false accept, and no crashes. No backend implementation changes or new resource-gate measurement in this batch.

- Declaration order: the complete checker suite and checked-in negative corpus pass. Three new rejection fixtures cover short, var, and const declarations; valid package/builtin fallback, block shadowing, and forward-label controls pass host/stage3. All 157 selected scope/shadowing/closure/goto corpus cases pass both compilers. Final preflight passes in 39 seconds. The refreshed original host audit records 93 checker diagnostics, 5 backend-only failures, one linker-only failure, one false accept, and no crashes. No backend implementation changes or new resource-gate measurement in this batch.

- Receive literals: the complete checker suite and checked-in negative corpus pass. Three rejection fixtures cover select, standalone, and string-literal receives; valid sends, parenthesized/indexed/directional receives, select values, and implicit-runtime controls pass host/stage3. Final preflight passes in 45 seconds. The refreshed original host audit records 94 checker diagnostics, 5 backend-only failures, one false accept, and no linker-only failures or crashes. No backend implementation changes or new resource-gate measurement in this batch.

- Conversion operands: the complete checker suite and checked-in negative corpus pass. Three rejection fixtures and positive string/byte/rune/named-slice, numeric, boolean, and shadowing controls pass host/stage3. A local function named `int` failed stage0 compilation before the backend resolution repair and now prints PASS; the added `string` shadowing control passed before and after. All 46 selected backend conversion/numeric-builtin programs pass direct-source and frontend-unit paths. Final preflight passes in 37 seconds. The refreshed original host audit records 97 checker diagnostics, 2 backend-only failures, one false accept, and no linker-only failures or crashes. The resource gate was rerun and still fails at 657,010 bytes against 327,680 bytes.

- Struct selectors: the complete checker/RTG suites and checked-in negative corpus pass. Three rejection fixtures cover literal, anonymous, and pointer-parameter selectors; nine member-access/interface/map controls pass host/stage3. Initial preflight exposed a missing ObjectImage method declaration in the embedded-Go checker API; the backend already implements this method, and the declaration is now synchronized with positive and unknown-method rejection tests. Final preflight passes in 52 seconds. The refreshed original host audit records 98 checker diagnostics, one backend-only failure, one false accept, and no linker-only failures or crashes. No backend implementation changes or new resource-gate measurement in this batch.

- Module version groundwork: preserve the main module's go directive in Module/Graph metadata; validate its spelling, duplicates, and extra arguments without truncating large version components. Loader tests cover release/prerelease versions and workspace retention. Two malformed/duplicate module fixtures and the complete negative corpus pass host/stage3, with errors now pointing to the offending directive. Three positive regressions pass both compilers; final preflight passes in 46 seconds. The original host audit remains 98 checker diagnostics, one backend-only failure, and one false accept. This does not yet enforce feature versions or implement new(expression); dependency manifests still need version propagation. No backend changes or new resource measurement.

- Dependency versions: collector manifests retain the logical module path and declared Go version, including replacements; the loader assigns module versions to main/dependency packages and still accepts historical path-only manifests. Unit tests verify transitive/replacement ownership, longest module-prefix selection, absent versions, and standard-library isolation. A three-module replacement regression and implicit-runtime control pass host/stage3, as does the complete negative corpus. Preflight passes in 52 seconds. The refreshed original audit remains 98 checker diagnostics, one backend-only failure, and one false accept. Feature gates, absent/standard-library version policy, and per-file version adjustments remain open; no backend changes or new resource measurement.

- File version groundwork: loaded Go files retain language versions derived from module declarations (patch/prerelease suffixes removed), the absent-module-directive default, and leading modern build constraints. Unit tests cover compound conjunction/disjunction/negation, upgrades/downgrades, the Go 1.21 floor, comments/BOM handling, and loaded-file retention. Dependency/runtime controls and the complete negative corpus pass host/stage3; final preflight passes in 38 seconds. The refreshed original audit remains 98 checker diagnostics, one backend-only failure, and one false accept. This metadata does not yet enforce feature gates; release-tag source selection and standard-library version policy remain open. No backend changes or new resource measurement.

- New expression version checking: checker/load/driver suites pass. Two negative fixtures and valid type-allocation/shadowing and dependency-version controls pass host/stage3; the complete checked-in negative corpus is green. Unit tests cover older/current/prerelease versions, per-file upgrades/downgrades, arity, and type/value distinctions. Final preflight passes in 50 seconds. The refreshed original 100-case host audit records 99 checker diagnostics and one false accept (`close(1)`), with no backend-only/linker-only failures or crashes. General operand typing and Go 1.26 expression allocation remain open; no backend changes or new resource measurement.

- Release-tag selection: canonical go1.1 through go1.25 tags are enabled using an explicit compiler baseline, independent of host or module versions; future tags require explicit user selection. Unit tests cover modern/legacy expressions, invalid spellings, large versions, and selected-file language metadata. A positive selection regression and per-file new-expression rejection pass host/stage3, along with dependency/type-allocation controls and the complete negative corpus. Final preflight passes in 51 seconds. The refreshed original host audit remains 99 checker diagnostics and one close(1) false accept, without backend/linker-only failures or crashes. Standard-library version policy, broader feature gates, and Go 1.26 allocation remain open. No backend changes or new resource measurement.

- Go object roots: the original probe still produced an empty 456-byte ELF after rebase; the existing writer only emitted functions reachable from explicit exports. Frontend object linking now seeds exported root-package functions and the generated main entrypoint, while preserving explicit names and C11 behavior. The original minimal probe produces a nonempty object. Permanent host/stage3 tests verify ELF code, global Add/main symbols, both object switches, and portable ELF linking. This exposed and repaired missing object-link dispatch in the embedded command frontend. Checker-adjacent linker/pipeline/driver tests, three focused backend export-ABI controls, and the full checked-in negative corpus pass. Final preflight passes in 48 seconds; the original host negative audit remains 99 checker diagnostics and one close(1) false accept. Linux execution/system linking, general library initialization and ABI coverage remain unverified/open. No backend implementation changes or new resource measurement.

- Incremental Go objects: a new regression first demonstrated missing exports in cached object sessions. The incremental path now shares root export selection and keys artifacts by executable/Go-object/C11-object policy. Source token lines are restored after artifact mapping, and merge line offsets use artifact token coordinates so removed source gaps cannot collapse EOF onto the last statement. Linker/pipeline tests cover warm cache reuse, mode isolation, dependency visibility, source preservation, and full serialized parity using the same built units (cached builds intentionally retain extra source fingerprints). Host/stage3 one-shot object/linking and full negative-corpus controls pass; final preflight passes in 51 seconds. The original host audit remains 99 checker diagnostics and one close(1) false accept. Cached-path runtime/extended transformation coverage and broader object support remain open. No backend changes or new resource measurement.

- Standard-library versions: files without their own version adjustment now default to the compiler baseline rather than an unset version or the importing module's directive. Loader tests verify module independence and tagged-file overrides; checker tests show that covered expression allocation is version-gated in standard sources under both old/new applications while new(Type) remains valid. Loader/checker/driver suites, three host/stage3 language-version controls, object/linking controls, and the complete negative corpus pass. Preflight passes in 49 seconds. The refreshed original host audit remains 99 checker diagnostics and one close(1) false accept. Validation ran with separate local test-temp-cleanup harness changes present; those changes are not included in this repair commit. Broader feature gates remain open; no backend changes or new resource measurement.

- Copy element compatibility: covered slice element identities are now compared before lowering, with the string-source exception restricted to byte/uint8 element identity rather than merely byte-like underlying types. Checker tests cover named types versus aliases, different named slice types, pointers/nested slices, globals/make operands, and shadowing. Three rejection fixtures and three positive copy/conversion controls pass host/stage3; the complete negative corpus is green after correcting initial fixture-column typos. Preflight passes in 49 seconds. The original host audit remains 99 checker diagnostics and one close(1) false accept. General operand/type resolution remains open; no backend changes or new resource measurement. Separate local test-cleanup harness edits remain excluded from this commit.


- Append expansion element compatibility: checker tests cover mismatched primitive, named, pointer, and nested-slice elements plus alias/shadowing controls. Three new rejection fixtures and three positive append/copy controls pass host/stage3; the complete checked-in negative corpus passes. Preflight passes in 55 seconds. The refreshed original host audit remains 99 checker diagnostics and one close(1) false accept, with no backend/linker-only failures or crashes. Ordinary appended values and general unresolved types remain open. No backend changes or new resource measurement; separate local test-cleanup harness edits remain excluded.

- Numeric/dot syntax: the scanner no longer invents a two-dot operator or consumes a second decimal point in leading-dot/hexadecimal literals. A parser diagnostic rejects covered adjacent dot punctuation after declaration recovery. Initial preflight exposed editor-completion regressions from overbroad dot validation; narrowing that pass and preserving declarations restored the existing checker/language-service tests. Three rejection fixtures (after correcting two expectation columns), three focused positive controls, and the complete negative corpus pass host/stage3. Final preflight passes in 49 seconds. The original host audit remains 99 checker diagnostics and one close(1) false accept. General expression grammar remains open; no backend changes or new resource measurement. Separate local test-cleanup harness edits remain excluded.

- Ordinary append scalar assignments: reject covered primitive-family mismatches, distinct typed scalar identities (including named types and numeric widths), nil into scalar elements, and direct fractional decimal literals into integer elements. Preserve untyped constants, aliases, byte/rune equivalence, and lexical shadowing. The shared numeric resolver now recognizes all integer widths. Checker tests, three new rejection fixtures, seven focused append/numeric/copy positives, and the complete negative corpus pass host/stage3. Preflight passes in 57 seconds. The original host audit remains 99 checker diagnostics and one close(1) false accept. General expression/aggregate assignment and full constant representability remain open. No backend changes or new resource measurement; separate local test-cleanup harness edits remain excluded.

- Append integer constant bounds: reuse multiword evaluation for resolved integer slice elements, rejecting fixed-width signed/unsigned overflow and negative unsigned values without machine-word truncation. Unit tables cover all fixed widths, named types/aliases, wide cancellation, package constants, and lexical shadowing. Three rejection fixtures, eight focused append/numeric/copy positive controls, and the complete negative corpus pass host/stage3. Preflight passes in 47 seconds; the original host audit remains 99 checker diagnostics and one close(1) false accept. Machine-sized types currently use the widest supported 64-bit bound; narrower target limits, general constant operations/scopes, and noninteger representability remain open. No backend changes or new resource measurement; separate local test-cleanup harness edits remain excluded.

- Multiword integer division/remainder: add immutable-operand binary long division with truncation toward zero and dividend-signed remainder, using 15-bit limbs safe on 32-bit checking compilers. Independent big-integer unit comparisons cover 396 large/sign combinations plus zero/exact/small cases; checker controls exercise array and append bounds and wide cancellation. Three rejection fixtures, three focused positive controls, and the complete negative corpus pass host/stage3. Preflight passes in 55 seconds; the original host audit remains 99 checker diagnostics and one close(1) false accept. This extends checker constant evaluation, not arbitrary-precision lowering or all remaining operations/scopes. No backend changes or new resource measurement; separate local test-cleanup harness edits remain excluded.

- Multiword bitwise constants: implement AND, OR, XOR, and bit-clear through sign-extended two's-complement limbs, preserving unbounded signed integer semantics and immutable operands. Independent big-integer unit comparisons cover 1,584 large/sign/operator combinations plus small/zero boundaries. Three rejection fixtures, three focused positive controls, and the complete negative corpus pass host/stage3; checker tests exercise wide array/append masks. Preflight passes in 45 seconds. The original host audit remains 99 checker diagnostics and one close(1) false accept. General constant scopes, noninteger evaluation, and arbitrary-precision lowering remain open. No backend changes or new resource measurement; separate local test-cleanup harness edits remain excluded.

- Rune literal checking: share exact rune decoding between scanning and multiword constant evaluation. Reject covered malformed rune bodies/escapes and rune constants outside integer append element bounds; preserve Unicode, byte/octal escapes, quote escapes, and rune arithmetic. Unit tests, three rejection fixtures (after correcting one diagnostic-line expectation), three focused positive controls, and the complete negative corpus pass host/stage3. The initial concurrent preflight run passed its checks but exceeded the budget at 73 seconds; a separate rerun passed in 36 seconds without changing any gate. The original host audit remains 99 checker diagnostics and one close(1) false accept. Floating-point constant semantics, general scopes, and arbitrary-precision lowering remain open. No backend changes or new resource measurement; separate local test-cleanup harness edits remain excluded.

- Escape accumulator bounds: check the byte/Unicode limit before each multiply/add so oversized eight-digit escapes cannot wrap a 32-bit checker integer before validation. Host syntax tests cover oversized, surrogate, and valid boundary escapes; the syntax test binary cross-compiles for Linux/386 (not executed here). Two focused positive controls and the complete negative corpus pass host/stage3. Preflight initially passed its checks but exceeded the budget at 62 seconds; the unchanged rerun passed in 39 seconds, with the bundled driver test returning from 27 seconds to about four. The original 100-case audit was not rerun for this guard-only change; its latest recorded result remains 99 checker diagnostics and one close(1) false accept. Native 32-bit execution and broader cross-target validation remain open. No backend changes or new resource measurement; separate local test-cleanup harness edits remain excluded.

- Numeric literal grammar: validate base-specific digits, underscore placement, decimal/hex exponents, and imaginary forms before emitting numeric tokens; reject binary/octal radix points instead of splitting them into apparently valid tokens. Unit controls preserve valid prefixed separators, hexadecimal floats, and decimal imaginary forms. Four rejection fixtures, three focused positive controls, and the complete negative corpus pass host/stage3. Final preflight passes in 49 seconds after the final boundary repair. The refreshed original host audit remains 99 checker diagnostics and one close(1) false accept. General expression grammar, numeric representability, and broader constant evaluation remain open. No backend changes or new resource measurement; separate local test-cleanup harness edits remain excluded.

- Package const groups: wide evaluation now repeats omitted expressions with the receiving specification's iota ordinal, counting specifications rather than individual names and preserving blank entries and group resets. Explicit non-iota constants retain a fast path. Unit controls cover multiple names, shadowing, and inherited expressions; two rejection fixtures, three focused positive controls, and the complete negative corpus pass host/stage3. Preflight passes in 48 seconds. The refreshed original host audit remains 99 checker diagnostics and one close(1) false accept. Local/typed constants, general constant semantics, and arbitrary-precision lowering remain open. No backend changes or new resource measurement; separate local test-cleanup harness edits remain excluded.
