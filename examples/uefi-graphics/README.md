# UEFI GOP graphics

This application locates the Graphics Output Protocol and writes a gradient
directly to RGB or BGR firmware framebuffers. It demonstrates protocol lookup,
mode inspection, pixel layout, and boot-service timing.
